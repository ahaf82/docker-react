language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ahaf82/docker-react -f Dockerfile.dev . 

script: 
  - docker run -e CI=true ahaf82/docker-react npm run test
